% standalone wrapper: compiles the tikzDevice output to a self-contained PDF.
% Build via `make` in this directory; the submission main.tex includes ONLY the PDF.
\documentclass[10pt]{standalone}
\usepackage{tikz}
\begin{document}
\input{figB_g_profiles.tex}
\end{document}
