% standalone wrapper: compiles the tikzDevice output to a self-contained PDF.
% Build via `make` in this directory; downstream documents include ONLY the PDF.
\documentclass[10pt]{standalone}
\usepackage{tikz}
\begin{document}
\input{figD_dose_response.tex}
\end{document}
